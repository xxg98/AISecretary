# MinIO

MinIO 用于本地模拟对象存储，可作为 `x-file-storage` 后续切换对象存储平台的候选。

默认地址：

```text
API:     http://localhost:9000
Console: http://localhost:9001
```

默认账号密码见：

```text
infrastructure/docker-compose/.env.example
```
